
loadstring(game:HttpGet(("https://raw.githubusercontent.com/Shade-vex/shadevex/refs/heads/main/menu-for-you-hub.txt")))()

       local Window = MakeWindow({
         Hub = {
         Title = "DZ hub [beta]",
         Animation = "Youtuber : Shade Vex"
         },
        Key = {
        KeySystem = fasle,
        Title = "DZ hub [beta]",
        Description = "đăng kí cho Shade-Vex đi",
        KeyLink = "https://discord.gg/DmN8sE7Y",
        Keys = {"Premium_Key_29833z920j8"},
        Notifi = {
        Notifications = true,
        CorrectKey = "Đang chạy script...",
       Incorrectkey = "Key không tồn tại",
       CopyKeyLink = "Đã sao chép link",
      }
    }
  })

       MinimizeButton({
       Image = "http://www.roblox.com/asset/?id=94011781668645",
       Size = {50, 50},
       Color = Color3.fromRGB(10, 10, 10),
       Corner = true,
       Stroke = false,
       StrokeColor = Color3.fromRGB(255, 0, 0)
      })
      
------ Tab
     local Tab1o = MakeTab({Name = "MAIN"})
     local Tab2o = MakeTab({Name = "FARM"})


------ Main

local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:FindFirstChild("Humanoid")

local godModeEnabled = false

AddButton(Tab1o, {
    Name = "NEVER DIE",
    Callback = function()
        godModeEnabled = not godModeEnabled
        if godModeEnabled then
            print("🔥 God Mode + Anti-Cheat: ON")

            -- **1️⃣ CHẶN MẤT MÁU**
            game:GetService("RunService").Stepped:Connect(function()
                if humanoid and godModeEnabled then
                    humanoid.Health = humanoid.MaxHealth
                end
            end)

            -- **2️⃣ CHẶN QUÁI TẤN CÔNG (Khiến quái bỏ qua bạn)**
            spawn(function()
                while godModeEnabled do
                    pcall(function()
                        for _, enemy in pairs(game.Workspace:GetChildren()) do
                            if enemy:IsA("Model") and enemy:FindFirstChild("Humanoid") then
                                enemy.Humanoid:ChangeState(Enum.HumanoidStateType.Seated) -- Vô hiệu hóa quái
                            end
                        end
                    end)
                    wait(1)
                end
            end)

            -- **3️⃣ CHẶN GAME XÓA NHÂN VẬT**
            character.AncestryChanged:Connect(function()
                if character.Parent == nil and godModeEnabled then
                    player.Character = character
                end
            end)

            -- **4️⃣ NGĂN GAME XỬ LÝ CÁI CHẾT**
            humanoid.BreakJointsOnDeath = false
            humanoid.RequiresNeck = false
            humanoid.Parent = nil
            wait(0.1)
            humanoid.Parent = character

            -- **5️⃣ TẠO NHÂN VẬT DỰ PHÒNG (Chống admin kill)**
            local clone = character:Clone()
            clone.Parent = workspace
            player.Character = clone

            -- **6️⃣ CHỐNG ANTI-CHEAT (Vượt qua kiểm tra bất tử)**
            spawn(function()
                while godModeEnabled do
                    pcall(function()
                        if humanoid then
                            humanoid:SetStateEnabled(Enum.HumanoidStateType.Dead, false)
                            humanoid:SetStateEnabled(Enum.HumanoidStateType.Physics, false)
                        end
                    end)
                    wait(1)
                end
            end)

            -- **7️⃣ VÔ HIỆU HÓA SCRIPT ANTI-CHEAT**
            spawn(function()
                for _, obj in pairs(getgc(true)) do
                    if type(obj) == "function" and islclosure(obj) then
                        local scriptName = getinfo(obj).source
                        if string.find(scriptName, "AntiCheat") then
                            setreadonly(obj, false)
                            disablefunction(obj)
                            print("🚫 Anti-Cheat đã bị vô hiệu hóa!")
                        end
                    end
                end
            end)

        else
            print("❌ God Mode: OFF")
        end
    end
})







