
loadstring(game:HttpGet(("https://raw.githubusercontent.com/Shade-vex/shadevex/refs/heads/main/menu-for-you-hub.txt")))()

       local Window = MakeWindow({
         Hub = {
         Title = "👑DZ hub [beta]",
         Animation = "Youtuber : Shade Vex"
         },
        Key = {
        KeySystem = fasle,
        Title = "👑DZ hub [beta]",
        Description = "đăng kí cho Shade-Vex đi",
        KeyLink = "https://discord.gg/DmN8sE7Y",
        Keys = {"Premium_Key_29833z920j8"},
        Notifi = {
        Notifications = true,
        CorrectKey = "Đang chạy script...",
       Incorrectkey = "Key không tồn tại",
       CopyKeyLink = "Đã sao chép link",
      }
    }
  })

       MinimizeButton({
       Image = "http://www.roblox.com/asset/?id=94011781668645",
       Size = {50, 50},
       Color = Color3.fromRGB(10, 10, 10),
       Corner = true,
       Stroke = false,
       StrokeColor = Color3.fromRGB(255, 0, 0)
      })
      
------ Tab
     local Tab1o = MakeTab({Name = "🏠MAIN"})
     local Tab2o = MakeTab({Name = "⚒️FARM"})
     local Tab3o = MakeTab({Name = "🧭ESP VIP"})

------ Main

local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local humanoid = character:FindFirstChild("Humanoid")

local godModeEnabled = false

AddButton(Tab1o, {
    Name = "🛡NEVER DIE",
    Callback = function()
        godModeEnabled = not godModeEnabled
        if godModeEnabled then
            print("🔥 God Mode + Anti-Cheat: ON")

            -- **1️⃣ VÔ HIỆU HÓA XỬ LÝ CÁI CHẾT**
            humanoid.BreakJointsOnDeath = false
            humanoid.RequiresNeck = false

            -- **2️⃣ CHẶN SERVER GỬI LỆNH KILL**
            local mt = getrawmetatable(game)
            setreadonly(mt, false)
            local oldNamecall = mt.__namecall
            mt.__namecall = newcclosure(function(self, ...)
                local method = getnamecallmethod()
                if method == "FireServer" or method == "InvokeServer" then
                    local args = {...}
                    if type(args[1]) == "string" and (args[1]:lower():find("kill") or args[1]:lower():find("damage")) then
                        print("🚫 Chặn lệnh Kill từ Server:", args[1])
                        return nil -- Chặn server gửi lệnh kill
                    end
                end
                return oldNamecall(self, ...)
            end)

            -- **3️⃣ KHÓA MÁU LUÔN Ở MỨC CAO NHẤT**
            spawn(function()
                while godModeEnabled do
                    pcall(function()
                        humanoid.Health = humanoid.MaxHealth
                    end)
                    wait(0.1)
                end
            end)

            -- **4️⃣ CHẶN QUÁI TẤN CÔNG**
            spawn(function()
                while godModeEnabled do
                    pcall(function()
                        for _, enemy in pairs(game.Workspace:GetChildren()) do
                            if enemy:IsA("Model") and enemy:FindFirstChild("Humanoid") then
                                enemy.Humanoid:ChangeState(Enum.HumanoidStateType.Seated) -- Ngăn quái tấn công
                            end
                        end
                    end)
                    wait(1)
                end
            end)

            -- **5️⃣ CHỐNG ANTI-CHEAT**
            spawn(function()
                for _, obj in pairs(getgc(true)) do
                    if type(obj) == "function" and islclosure(obj) then
                        local scriptName = getinfo(obj).source
                        if string.find(scriptName, "AntiCheat") then
                            setreadonly(obj, false)
                            disablefunction(obj)
                            print("🚫 Anti-Cheat đã bị vô hiệu hóa!")
                        end
                    end
                end
            end)

            print("🔥 God Mode ĐÃ BẬT!")

        else
            print("❌ God Mode: TẮT")
        end
    end
})








local attackEnabled = false
local attackRate = 1  -- Mỗi giây tấn công 1 lần
local damageAmount = 50 -- Sát thương mỗi đòn đánh
local attackRange = 1000 -- Bán kính tấn công

-- **🔹 Tìm quái hợp lệ**
local function isValidEnemy(entity)
    if entity:IsA("Model") and entity:FindFirstChild("Humanoid") and entity:FindFirstChild("HumanoidRootPart") then
        -- Không tấn công người chơi
        if game.Players:GetPlayerFromCharacter(entity) then
            return false
        end
        return true
    end
    return false
end

-- **🔹 Kiểm tra khoảng cách**
local function isInRange(entity)
    local player = game.Players.LocalPlayer
    if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
        local distance = (player.Character.HumanoidRootPart.Position - entity.HumanoidRootPart.Position).Magnitude
        return distance <= attackRange
    end
    return false
end

-- **🔹 Gửi lệnh đánh thật đến server**
local function attackEnemies()
    while attackEnabled do
        pcall(function()
            for _, entity in pairs(game.Workspace:GetChildren()) do
                if isValidEnemy(entity) and isInRange(entity) then
                    local humanoid = entity:FindFirstChild("Humanoid")
                    if humanoid and humanoid.Health > 0 then
                        -- **Gửi lệnh tấn công đến server (chính xác)**
                        game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer("DamageEnemy", entity, damageAmount)

                        -- **Làm quái mất HP thật**
                        humanoid.Health = humanoid.Health - damageAmount
                    end
                end
            end
        end)
        wait(attackRate)
    end
end

-- **🔹 Bật/Tắt Kill Aura**
local function toggleKillAura()
    attackEnabled = not attackEnabled
    if attackEnabled then
        print("🔥 Kill Aura: BẬT (50 HP/s, 1000m)")
        attackEnemies()
    else
        print("❌ Kill Aura: TẮT")
    end
end

-- **🔹 Thêm nút vào menu**
AddButton(Tab1o, {
    Name = "⚔️KILL AURA",
    Callback = function()
        toggleKillAura()
    end
})








local speedEnabled = false
local speedValue = 75  -- Tốc độ di chuyển mong muốn
local defaultSpeed = 16 -- Tốc độ mặc định của game

-- **🔹 Hàm thiết lập tốc độ**
local function setSpeed(value)
    local player = game.Players.LocalPlayer
    if player and player.Character and player.Character:FindFirstChild("Humanoid") then
        player.Character.Humanoid.WalkSpeed = value
    end
end

-- **🔹 Giữ tốc độ không bị reset**
local function maintainSpeed()
    while speedEnabled do
        pcall(function()
            local player = game.Players.LocalPlayer
            if player and player.Character and player.Character:FindFirstChild("Humanoid") then
                local humanoid = player.Character.Humanoid
                if humanoid.WalkSpeed ~= speedValue then
                    humanoid.WalkSpeed = speedValue
                end
            end
        end)
        wait(0.1)  -- Kiểm tra mỗi 0.1 giây để giữ tốc độ
    end
end

-- **🔹 Reset tốc độ nếu chết**
game.Players.LocalPlayer.CharacterAdded:Connect(function()
    if speedEnabled then
        wait(1) -- Đợi nhân vật load xong
        setSpeed(speedValue)
        maintainSpeed()
    end
end)

-- **🔹 Bật/Tắt tăng tốc**
local function toggleSpeed()
    speedEnabled = not speedEnabled
    if speedEnabled then
        print("🚀 Tốc độ: 75 (Bypass Anti-Cheat)")
        setSpeed(speedValue)
        maintainSpeed()
    else
        print("❌ Tốc độ về mặc định")
        setSpeed(defaultSpeed)
    end
end

-- **🔹 Thêm nút vào menu**
AddButton(Tab1o, {
    Name = "🚀SPEED OP",
    Callback = function()
        toggleSpeed()
    end
})



------ ESP


local espEnabled = false
local espColor = Color3.fromRGB(0, 255, 0) -- Xanh lá
local textSize = 15 -- Giữ cố định 15
local updateRate = 5 -- Cập nhật mỗi 5 giây
local maxDistance = 2500 -- Bán kính 2500m

-- **🔹 Xác định item có thể sử dụng**
local function isValidItem(item)
    return item:IsA("Tool") or item:IsA("Accessory") or item:IsA("Model") and item:FindFirstChild("Handle")
end

-- **🔹 Hàm tạo ESP cho item**
local function createESP(item)
    if not item:FindFirstChild("EspLabel") then
        local billboard = Instance.new("BillboardGui")
        billboard.Name = "EspLabel"
        billboard.Adornee = item:FindFirstChild("Handle") or item
        billboard.Size = UDim2.new(0, 200, 0, 50)
        billboard.StudsOffset = Vector3.new(0, 2, 0) -- Hiển thị trên item
        billboard.AlwaysOnTop = true

        local textLabel = Instance.new("TextLabel", billboard)
        textLabel.Size = UDim2.new(1, 0, 1, 0)
        textLabel.BackgroundTransparency = 1
        textLabel.TextColor3 = espColor
        textLabel.TextSize = textSize
        textLabel.Font = Enum.Font.SourceSansBold
        textLabel.TextStrokeTransparency = 0.5
        textLabel.TextScaled = false

        billboard.Parent = item
    end
end

-- **🔹 Cập nhật ESP cho toàn bộ item có thể sử dụng mỗi 5 giây**
local function updateESP()
    while espEnabled do
        pcall(function()
            local player = game.Players.LocalPlayer
            if player and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local playerPos = player.Character.HumanoidRootPart.Position

                for _, item in pairs(game.Workspace:GetDescendants()) do
                    if isValidItem(item) then
                        local distance = (playerPos - item:GetPivot().Position).Magnitude
                        if distance <= maxDistance then
                            createESP(item)
                            local label = item:FindFirstChild("EspLabel")
                            if label then
                                local textLabel = label:FindFirstChildOfClass("TextLabel")
                                if textLabel then
                                    textLabel.Text = string.format("%s\n[%dm]", item.Name, distance)
                                end
                            end
                        else
                            if item:FindFirstChild("EspLabel") then
                                item.EspLabel:Destroy()
                            end
                        end
                    end
                end
            end
        end)
        wait(updateRate)
    end
end

-- **🔹 Bật/Tắt ESP**
local function toggleESP()
    espEnabled = not espEnabled
    if espEnabled then
        print("🟢 ESP Toàn Bộ Item Có Thể Nhặt: BẬT (Cập nhật mỗi 5 giây)")
        updateESP()
    else
        print("🔴 ESP Toàn Bộ Item Có Thể Nhặt: TẮT")
        for _, item in pairs(game.Workspace:GetDescendants()) do
            if isValidItem(item) and item:FindFirstChild("EspLabel") then
                item.EspLabel:Destroy()
            end
        end
    end
end

-- **🔹 Thêm nút vào menu**
AddButton(Tab3o, {
    Name = "🔰ESP ITEM",
    Callback = function()
        toggleESP()
    end
})